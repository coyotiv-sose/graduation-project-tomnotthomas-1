//with this function I want to add an organisation.

let newOrganisation = []

function addOrganisation(organisation) {
  const organisation = new Organisation(name, owner, id)
  newOrganisation.push(organisation)
}

module.exports = {
  newOrganisation,
  addOrganisation,
}
